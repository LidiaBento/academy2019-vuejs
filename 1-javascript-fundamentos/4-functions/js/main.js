/*
  soma
  subtrai
  multiplica
  divide
  full-name
*/

let primeiroValor = 2
let segundoValor = 4
function soma(primeiroValor + segundoValor) {
  return `Soma, ${soma}`
}

function subtrai(primeiroValor - segundoValor){
return `Subtração, ${subtrai}`
)

function multiplica(primeiroValor*segundoValor){
  return `Multiplicação, ${multiplica}`
}

function divide(primeiroValor / segundoValor){
  return `Dividir, ${divide}`
}

let firstName = Lidia
let lastName = Bento
function nomeCompleto(firstName + lastName){
return `Nome Completo, $(nomeCompleto)`
}

//function minhaFuncao(name) {
  //return `Olá, ${name}`
//}

// const minhaFuncao = function (name) {
//   return `Olá, ${name}`
// }

// const minhaFuncao = name => `Olá, ${name}`
// => significa o return 

//console.log(minhaFuncao())